# RehabQR - تطبيق مسح الرموز QR

تطبيق موبايل لمسح رموز QR وإدارة السجلات محلياً.

## الميزات الرئيسية

- 🔐 **نظام تسجيل الدخول**: تسجيل دخول آمن مع حفظ الجلسة
- 🔑 **استرجاع كلمة المرور**: استرجاع كلمة المرور عبر رقم الهاتف
- 📱 **مسح رموز QR**: مسح فوري بواسطة الكاميرا
- 📋 **إدارة السجلات**: حفظ وعرض السجلات محلياً
- 🎨 **واجهة عربية**: دعم كامل RTL مع تصميم عصري
- ⚙️ **الإعدادات**: إدارة اللغة، معلومات الحساب، والإصدار
- 💾 **حفظ محلي**: حفظ السجلات باستخدام AsyncStorage

## التقنيات المستخدمة

- **Expo React Native** - إطار العمل الرئيسي
- **Expo Router** - التنقل بين الصفحات
- **Expo Camera** - مسح رموز QR
- **Expo Image** - عرض الصور واللوجو
- **React Native Paper** - مكونات UI
- **React Query** - إدارة البيانات
- **AsyncStorage** - التخزين المحلي
- **MaterialCommunityIcons** - الأيقونات

## البدء

### التثبيت

```bash
npm install
```

### تشغيل التطبيق

```bash
npm start
```

ثم اختر:
- `a` لتشغيل على Android
- `i` لتشغيل على iOS
- `w` لتشغيل على المتصفح

## البنية الأساسية

```
app/
├── login.tsx          # صفحة تسجيل الدخول
├── forgot-password.tsx # صفحة استرجاع كلمة المرور
├── verify-otp.tsx     # صفحة التحقق من رمز OTP
├── reset-password.tsx # صفحة إنشاء كلمة مرور جديدة
├── (tabs)/
│   ├── index.tsx      # صفحة المسح (الرئيسية)
│   ├── records.tsx    # صفحة السجل
│   └── settings.tsx   # صفحة الإعدادات
├── _layout.tsx        # التخطيط الرئيسي مع RTL

components/
├── ui/
│   ├── Button.tsx     # زر مخصص
│   ├── Card.tsx       # بطاقة مخصصة
│   ├── Input.tsx      # حقل إدخال
│   ├── Typography.tsx # نصوص منسقة
│   └── Toast.tsx      # إشعارات
└── AuthGuard.tsx      # حماية الصفحات بالمصادقة

contexts/
├── AuthContext.tsx    # إدارة المصادقة
└── StorageContext.tsx # إدارة التخزين المحلي

constants/
└── theme.ts           # نظام الألوان والتصميم

assets/
└── images/
    └── Logo.svg       # لوجو التطبيق
```

## الصفحات

### 1. صفحة تسجيل الدخول (Login)
- عرض لوجو التطبيق في أعلى الصفحة
- تسجيل دخول برقم الهاتف وكلمة المرور
- إظهار/إخفاء كلمة المرور
- رابط إلى استرجاع كلمة المرور
- واجهة عربية كاملة مع تصميم عصري
- توجيه تلقائي إلى الصفحة الرئيسية عند نجاح تسجيل الدخول

### 2. صفحة استرجاع كلمة المرور (Forgot Password)
- إدخال رقم الهاتف
- إرسال رمز إعادة التعيين
- التنقل إلى صفحة التحقق من OTP
- لا يوجد تسجيل - استرجاع فقط

### 3. صفحة التحقق من OTP (Verify OTP)
- إدخال رمز التحقق المؤلف من 4 أرقام
- حقول إدخال منفصلة مع انتقال تلقائي
- زر تحقق معطَّل حتى إكمال جميع الأرقام
- مؤشر تحميل أثناء التحقق
- زر إعادة إرسال مع عداد زمني (60 ثانية)
- رابط تغيير رقم الجوال
- رسائل خطأ ونجاح مفصلة
- واجهة عربية كاملة مع RTL
- دعم الوصول (Accessibility) كامل

### 4. صفحة إعادة تعيين كلمة المرور (Reset Password)
- إدخال كلمة مرور جديدة
- تأكيد كلمة المرور
- إظهار/إخفاء كلمة المرور لكلا الحقلين
- التحقق من تطابق كلمة المرور
- التحقق من قوة كلمة المرور (8 أحرف على الأقل)
- مؤشر تحميل أثناء الحفظ
- رسائل خطأ ونجاح مفصلة
- توجيه تلقائي إلى صفحة تسجيل الدخول بعد النجاح
- واجهة عربية كاملة مع RTL

### 5. صفحة المسح (Scanner)
- فتح الكاميرا لمسح رموز QR
- إطار توجيه بصري للمسح
- نصائح الاستخدام
- حفظ السجلات تلقائياً

### 6. صفحة السجل (Records)
- عرض جميع السجلات المحفوظة
- بطاقة لكل سجل مع تفاصيل كاملة
- حالة فارغة عند عدم وجود سجلات
- عرض نصوص RTL

### 7. صفحة الإعدادات (Settings)
- معلومات الحساب (الاسم، الهاتف)
- تبديل اللغة (عربي/إنجليزي)
- رقم الإصدار (v 1.1.8)
- زر تسجيل الخروج مع تأكيد
- توجيه تلقائي إلى صفحة تسجيل الدخول بعد الخروج

## الإذونات

التطبيق يطلب:
- **الكاميرا**: لمسح رموز QR

## التصميم

- نظام ألوان مطابق للتصميم الأصلي
- لوجو احترافي في صفحة تسجيل الدخول
- دعم كامل RTL
- أيقونات Material Design
- تصميم responsive
- ظلال وخطوط سلسة
- خطوط عربية مناسبة مع دعم FontWeights (light, normal, bold, extrabold)

